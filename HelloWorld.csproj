<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net9.0</TargetFramework>
    <!-- changing the net9.0 to other numbers and entering dotnetbuild in terminal it will build the app to the current build.  -->
  </PropertyGroup>

</Project>
